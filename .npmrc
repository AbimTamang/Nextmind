# Workspace packages resolve to the local source, never to a registry copy.
link-workspace-packages=true
# Fail loudly when an app imports something it did not declare - the usual way a
# monorepo rots is via accidental reliance on a hoisted transitive dependency.
strict-peer-dependencies=false
auto-install-peers=true
